/target
.env
*.env
.DS_Store
/temp
/graphify-out
/.claude

# Build outputs (Rust target dirs at any depth, Trunk dist, Zola output)
target/
/website/dist
/docs/public
# Local tooling / scratch artifacts — keep out of the repo
*.db
*.rlib
.mcp.json
.graphifyignore